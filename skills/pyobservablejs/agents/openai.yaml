interface:
  display_name: pyobservablejs
  short_description: Build and diagnose Observable charts from Python
  default_prompt: Use $pyobservablejs to work in the active notebook environment,
    verify browser evaluation, inspect structured diagnostics and chart data, and
    validate the rendered result.
