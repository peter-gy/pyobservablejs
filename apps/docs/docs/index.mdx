---
title: pyobservablejs
sidebar_label: Overview
description: Observable JavaScript notebooks from Python, with dataflow in both directions.
slug: /
---

import DocCardList from "@theme/DocCardList";

# pyobservablejs

`pyobservablejs` renders [Observable JavaScript notebooks](https://observablehq.com/notebook-kit/)
from Python and connects the two runtimes. It does three things:

1. **Creates notebooks.** Author [Notebook Kit](https://observablehq.com/notebook-kit/)
   cells from Python, load existing Notebook Kit HTML, or import public
   observablehq.com notebooks.
2. **Moves values in both directions.** Python variables drive the reactive
   graph in the browser, and rendered browser values synchronize back to
   Python.
3. **Exposes the dataflow graph.** Python can inspect which cell defines each
   variable, follow dependency edges, and export Mermaid or D2 diagrams.

Each view is an [anywidget](https://anywidget.dev/), so notebooks render in
any anywidget host: JupyterLab, marimo, VS Code notebooks, Google Colab, and
others. The live examples on this site run in marimo.

Hover a dot to inspect a measurement. `Plot` and the `penguins` sample ship
with Notebook Kit.

```marimo-config
requires-python = ">=3.11"
dependencies = ["pyobservablejs"]
```

```python
import marimo as mo
import observablejs as obs

notebook = obs.Notebook(
    obs.js(
        """
        Plot.dot(penguins, {
          x: "culmen_length_mm",
          y: "culmen_depth_mm",
          fill: "species",
          r: 4,
          tip: {fill: "var(--marimo-island-background)"}
        }).plot({
          height: 320,
          color: {legend: true},
          x: {grid: true, label: "Bill length (mm)"},
          y: {grid: true, label: "Bill depth (mm)"}
        })
        """
    ),
)

full_view = notebook.view()
mo.ui.anywidget(full_view)
```

```python marimo echo=false server-output=false
import marimo as mo
import observablejs as obs

notebook = obs.Notebook(
    obs.js(
        """
        Plot.dot(penguins, {
          x: "culmen_length_mm",
          y: "culmen_depth_mm",
          fill: "species",
          r: 4,
          tip: {fill: "var(--marimo-island-background)"}
        }).plot({
          height: 320,
          color: {legend: true},
          x: {grid: true, label: "Bill length (mm)"},
          y: {grid: true, label: "Bill depth (mm)"}
        })
        """
    ),
)

full_view = notebook.view()
mo.ui.anywidget(full_view)
```

## Where to go next

<DocCardList
	items={[
		{
			type: "link",
			href: "/quickstart/",
			label: "Quickstart",
			description: "Install pyobservablejs and render your first reactive notebook.",
		},
		{
			type: "link",
			href: "/mental-model/",
			label: "Mental model",
			description: "Notebook, NotebookCell, and NotebookView: who owns what.",
		},
		{
			type: "link",
			href: "/connect/python-to-observable/",
			label: "Connect Python and the browser",
			description: "Drive the reactive graph from Python and read values back.",
		},
		{
			type: "link",
			href: "/pyobservablejs-or-pyobsplot/",
			label: "pyobservablejs or pyobsplot?",
			description: "Choose the right tool for Observable-flavored work from Python.",
		},
	]}
/>
