---
title: Troubleshooting
description: Symptoms, causes, and fixes for common pyobservablejs failures.
---

# Troubleshooting

Start with the symptom. Each entry names the contract behind the fix.

## `NotRenderedError` when reading values or the graph

Readback exists only after the browser produces it. `values` and
`cell_values` require a completed render. `graph` requires a graph snapshot.
Guard with the gates:

```python
if full_view.has_rendered:
    values = full_view.values
if full_view.has_graph_snapshot:
    graph = full_view.graph
```

The gates are per-view: rendering one view does not unlock a sibling. They
also reset while a `replace_variables` or `reset_variables` rebuild is in
flight. See [Manage view lifecycle](render/view-lifecycle.mdx).

## The view displays nothing

- **marimo:** wrap the view with `mo.ui.anywidget(full_view)`. marimo renders
  the returned UI element.
- **Jupyter and most hosts:** the view must be the final expression of a
  cell, or passed to `display(...)`.
- **Colab:** enable third-party widget support, per
  [anywidget's Colab notes](https://anywidget.dev/en/notebooks/colab/).
- **Program cells:** a JavaScript cell with statements renders nothing
  unless it calls `display(...)` or `view(...)`. Expression cells display
  implicitly. See [Author cells from Python](create/author-cells.mdx#javascript-cells).

## The same view appears twice and one copy froze

A `NotebookView` owns one live writable output. Mounting the same view
object in two places leaves only one output writable. Create one view per
placement. The views stay synchronized through the session. See [Display a
NotebookView](render/display-views.mdx#one-view-per-placement).

## `ValueError` for a variable name

Variable names must match `^[A-Za-z_$][0-9A-Za-z_$]*$`, and runtime-owned
builtin names such as `penguins`, `Plot`, or `width` are reserved. Rename
the Python variable. The reserved list is in [Variables and
serialization](reference/variables-and-serialization.mdx#variable-names).

## `TypeError: Value of type ... is not serializable`

The [serialization
table](reference/variables-and-serialization.mdx#python-to-browser) lists
the types that cross the wire. Convert custom objects to plain mappings,
lists, or DataFrames first. Use [file
attachments](create/files-and-modules.mdx) for large tables and binary data.

## A name is missing from `values`

`values` omits names produced by more than one selected cell because no
unique owner exists. Read the value per cell with `cell_values`, or
restructure the notebook so one cell defines the name.

## The chart did not react to `update_variables`

- The update is a no-op when the serialized value is unchanged. Mutating a
  list in place and passing the same object still changes the wire value
  only if the contents differ.
- The cell must reference the variable by that exact name. Check
  `full_view.graph.external_references` to see what the cells actually
  resolve from the environment. See [Inspect the dependency
  graph](connect/dependency-graph.mdx).

## A marimo cell shows stale readback

marimo reruns a cell when a UI element it references changes. Reference the
wrapped widget's `value` in the reading cell:

```python
widget = mo.ui.anywidget(full_view)
```

```python
widget.value  # subscribe
full_view.values if full_view.has_rendered else None
```

## `TraitError` when assigning `notebook.theme`

Source-backed notebooks take their theme from the source HTML. Edit the
`<notebook theme="...">` attribute in the document. See [Themes and
pinned source](customize/themes-and-pinned-source.mdx#theme-names).

## `RuntimeError: Cannot mutate a closed Notebook`

`notebook.close()` closes the session and every view. `view.close()` closes
one view. After closing, variable mutators and `view()` raise `RuntimeError`.
Cell handles, attachments, variables, and theme stay readable. Recreate the
notebook to continue.

## `require is not defined` in an authored notebook

`require` belongs to the classic Observable standard library selected by
ObservableHQ imports. Python-authored and Notebook Kit HTML notebooks use
the Notebook Kit profile. Import npm packages with
`import ... from "npm:..."`. See [Browser execution and network
access](customize/browser-execution.mdx#runtime-profiles).

## Imports or datasets fail to load in a restricted environment

npm imports, URL-backed attachments, and ObservableHQ imports load from the
network in the browser at render time, and the page content security policy
must permit those origins. Embed local modules and files with
`rewrite_imports=True` and `embed_file_attachments=True` to reduce runtime
fetches. See [Browser execution and network
access](customize/browser-execution.mdx#module-imports-and-the-network).

## `OSError` or `ValueError` from `from_observablehq`

The constructor fetches the document API on every call: network failures
raise `OSError`, invalid specifiers and non-JSON responses raise
`ValueError`. Only public notebooks are reachable. Cache the document once
and use `from_observablehq_document` in builds and tests. See [Import
ObservableHQ notebooks](create/observablehq.mdx).
